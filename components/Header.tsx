import Logo from "./Logo"

const Header = () => {
  return (
    <header>
        <nav>
            <Logo />
            <div>
                {/* language select */}
                {/* session */}

            </div>
        </nav>
        {/* subscribe */}
    </header>
  )
}

export default Header